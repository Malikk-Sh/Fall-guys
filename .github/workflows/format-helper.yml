name: Temporary format helper

on:
  push:
    branches:
      - agent/race-server-auto-finish

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          ref: agent/race-server-auto-finish
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install exact dependencies
        run: npm ci
      - name: Apply Prettier
        run: npx prettier --write server/raceServerAutoFinish.test.mjs
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/raceServerAutoFinish.test.mjs
          git commit -m "Apply Prettier formatting"
          git push origin HEAD:agent/race-server-auto-finish
