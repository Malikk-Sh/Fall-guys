name: Diagnose network auth tests

on:
  push:
    branches:
      - agent/network-auth-hardening

permissions:
  contents: write

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/network-auth-hardening
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Capture npm test failure context
        shell: bash
        run: |
          set +e
          npm test > /tmp/network-auth-test.log 2>&1
          code=$?
          grep -n -B 40 -A 80 '^not ok' /tmp/network-auth-test.log > ci-network-auth-failure.txt || true
          echo "exit_code=$code" >> ci-network-auth-failure.txt
          exit 0
      - name: Commit diagnostic context
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f ci-network-auth-failure.txt
          git commit -m "Capture exact network auth test failure"
          git push origin HEAD:agent/network-auth-hardening
